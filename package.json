{
  "name": "suggested-notes-plugin",
  "version": "0.4.0",
  "description": "Sidebar plugin for Obsidian that suggests related notes based on shared tags, links, and body tokens.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "typecheck": "tsc -noEmit -skipLibCheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "report:tokenizer-contrast": "PRINT_TOKENIZER_CONTRAST_REPORT=1 vitest run src/util/tokenizeContrast.report.test.ts --disableConsoleIntercept"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "suggested-notes"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3",
    "vitest": "^4.1.6"
  },
  "dependencies": {
    "tiny-segmenter": "^0.2.0"
  }
}
